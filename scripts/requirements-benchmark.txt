# LongMemEval benchmark dependencies
# Install with: pip install -r scripts/requirements-benchmark.txt
sentence-transformers>=3.0.0
psycopg2-binary>=2.9.0
openai>=1.0.0          # optional, only needed for Phase 3 QA eval
